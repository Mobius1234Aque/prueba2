name: Run Tests

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run tests with AWS CodeBuild
        env:
          AWS_REGION: us-west-2
          AWS_ACCESS_KEY_ID: ${{ secrets.AKIAVKG5SLESZV5HRHE4 }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.6Rg7FnuUrzFBRx4Ug+acYyAUYbYZ9QUdF260/sL9 }}
        run: |
          aws codebuild start-build --project-name protyoosos --environment-variables-override "name=NODE_ENV,value=testing"
